<!-- This is just a placeholder since we can't actually create a binary .ico file through the text interface -->
<!-- In a real project, you would convert the SVG to an ICO file and place it here -->
